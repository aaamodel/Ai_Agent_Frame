## 1. 依赖与基础设施

- [x] 1.1 在 `requirements.txt` 增加分词依赖（`jieba`），并确认安装后为纯本地调用（无网络、无模型请求）；验证：安装后单测可 import 且分词结果符合预期
- [x] 1.2 新增停用词表与分词封装：输入文本 → 核心词集合；验证：单测断言业务术语被保留、常见停用词被过滤

## 2. 候选方向列表的确定性生成（全部由代码完成）

- [x] 2.1 已用工具集合的推导：从 `subtask_results` 提取 `tool_name`；验证：单测断言多次执行后集合完整，且记录缺字段时不报错
- [x] 2.2 **差集①（工具侧）＝ `state["active_tool_names"]` − 本轮已调用工具**；验证：单测断言已调用过的工具不再出现在候选中
- [x] 2.3 **差集②（资产侧）＝ 已提取的结构化事实 − 本轮已尝试过的资产**。判定"已尝试"用**反查**而非抽取路径：把每条 `action_input` 的值拼成文本，若某资产的位置或其文件名出现在其中即判为已尝试（分隔符与大小写归一化，完整位置与文件名同时匹配）；验证：单测断言不需要知道参数名即可判定，且"仅文件名命中"也被判为已尝试
- [x] 2.4 白名单求交：需要白名单外工具才能消费的资产 MUST NOT 进候选，并留下告警（含资产名与原因）；验证：单测断言白名单无对应工具时该资产被排除且告警内容完整
- [x] 2.5 候选渲染：每条含短标识 + 一句话描述，总长受上限约束（约 300 字符）；验证：单测断言渲染文本含标识与描述，且超限时被截断
- [x] 2.6 两个差集均为空时不注入且不中断；验证：单测断言空集时不产生候选段，流程按既有逻辑继续

## 3. 注入时机（只在需要时注入）

- [x] 3.1 本步工具调用未取得有效数据（错误 / 空数据）时注入；验证：单测断言该情形下下一步提示词含候选段
- [x] 3.2 上一步自评为「未解决」或「部分解决」时注入；验证：单测断言该情形下本步提示词含候选段
- [x] 3.3 正常成功且上一步自评为已解决时不注入；验证：单测断言该情形下提示词不含候选段

## 4. 模型只选不造

- [x] 4.1 `SubTaskOutcomeSchema` 增加可选字段 `selected_alternative_id`（输出候选项标识，不选则为 null）；验证：单测断言该字段可选——缺它的旧响应仍能正常解析
- [x] 4.2 系统翻译：按 `asset:` / `tool:` 前缀把标识翻译为具体动作，并对结果做一次白名单校验；验证：单测覆盖两种前缀的翻译结果正确，且翻译出的工具不在白名单时被拒绝
- [x] 4.3 标识不在本次候选列表中、或纠偏指令无法解析时，忽略该指令并按既有口径降级为「继续执行下一个子任务」；验证：单测断言降级后本步已产出的结论不被丢弃

## 5. 证据缺口检测（比对数据集 A 与数据集 B）

- [x] 5.1 **数据集 A（问题侧核心词）**：按优先级从「本轮目标 / 子问题 / 意图树命中路径 / 已提取事实」抽取，均缺失时回退到改写后的问题；经分词 + 停用词过滤产出核心词集合；验证：单测断言优先级生效，且可用来源全空时回退到问题文本
- [x] 5.2 **数据集 B（本步返回内容）**：取本步 `observation`；比对只针对本步，不掺入历史结论；验证：单测断言历史结论中的高覆盖不会把本步的跑题掩盖掉
- [x] 5.3 覆盖率与可配置阈值：`coverage = |{w ∈ A : w 出现在 B 中}| / |A|`，低于阈值即置位；验证：单测覆盖"不匹配→置位""匹配→不置位"
- [x] 5.4 两条不置位条件：A 为空 → 不置位；B 为空（纯推理步骤或工具未返回文本）→ 不置位；验证：单测覆盖两种输入
- [x] 5.5 确认该检测不产生任何模型调用；验证：单测中桩住模型调用并断言调用次数为 0

## 6. 闸门与留痕

- [x] 6.1 配额闸门：单轮纠偏次数不超过剩余调用预算的一半，耗尽即关闭该能力；验证：单测覆盖"配额内→生效""配额为零→忽略并降级"
- [x] 6.2 作用域闸门：应用纠偏时只改 cursor 之后的计划项，cursor 保持不变，已跳过记录与提前收尾标记不清空；验证：单测断言应用后 cursor 与跳过记录保持原样
- [x] 6.3 纠偏留痕：含触发条件、选中的候选项标识、应用后的目标动作、剩余配额；验证：单测断言四项均可查到

## 7. 两个核心算法的专项测试（可独立运行）

> 这两个算法是本变更的关键，必须有**独立的、可直接看出输入输出的专项测试**，而不是混在集成测试里。

- [x] 7.1 **资产差集专项测试**（`测试/test_step_correction_candidates.py`）：以真实 SKILL.md 的「数据资产地图」结构构造 `extracted_assets`（名称 / 位置 / 消费工具），以若干条含真实 `action_input` 的执行记录构造"本轮已尝试"。断言：① 未出现在任何 `action_input` 中的资产全部进入候选；② 位置被调用过的资产被剔除；③ 仅文件名命中也被判为已尝试；④ 白名单不含消费该资产的工具时被排除并留下告警；⑤ 两个差集均为空时不注入且不中断。验证：`pytest 测试/test_step_correction_candidates.py -q` 全部通过
- [x] 7.2 **证据缺口检测专项测试**（`测试/test_step_correction_evidence_gap.py`）：固定数据集 A（用分词从本次 trace 的问题与子问题抽出的核心词，如"行业""优先级"）与数据集 B（trace 中真实的工具返回文本，至少含"返回销售记录"与"知识库无匹配"两类）。断言：① B 中不含 A 的核心词时置位（销售记录 vs 行业优先级）；② B 含 A 的核心词时不置位；③ A 为空时不置位；④ B 为空时不置位；⑤ 覆盖率不掺入历史结论。验证：`pytest 测试/test_step_correction_evidence_gap.py -q` 全部通过

## 8. 回归与端到端

- [x] 8.1 回归全部现有测试；验证：`pytest evals 测试 benchmark -q` 通过，且与变更前基线一致（当前存在 1 个既有的 cost gate 失败项，非本变更引入，不计入）
- [x] 8.2 用本次 trace 的原始问题端到端实跑一次；验证：记录候选列表出现在哪些步骤、重规划次数、大模型调用次数与各次输入字符数，与变更前（13 次调用 / 约 37,400 字符 / 2 次重规划）对比
- [x] 8.3 构造"取到了数据但数据不对"的场景（返回销售记录而问题问行业优先级）；验证：证据缺口信号置位、候选列表被注入、模型选中后按候选换源，且不再重复已证否的路径


---

## 9. 实现后补记（实测证据）

- 8.2 的真实端到端已跑通（Milvus 可用）。**强制 plan_execute** 跑本次 trace 的原始问题：
  success=True、degraded=False、答案非空；
  **4 次大模型调用 / 9,942 输入字符 / 0 次重规划**，对比变更前 **13 次 / 约 37,400 字符 / 2 次重规划**
  （调用数 -69%，输入字符 -73%）。
- 初始用 strategy=auto 跑时落在 **react** 模式（那是 ix-agent-context-loss 修复
  "优先→先"误命中后的结果），而本变更的纠偏挂在 **plan 分支**上，因此 8.2 必须显式
  mode="plan_execute" 才能覆盖到纠偏路径。
- 8.3 的图级端到端（	est_evidence_gap_lets_model_switch_data_source_midflight）证明：
  证据缺口置位 → 候选列表注入 → 模型选中 → **第二步真的改用了 lt_tool**
  （原计划的 	2_tool 未被调用）；对照实验（	est_no_injection_when_step_is_healthy...）
  证明不满足注入条件时标识被忽略、计划原样不动。
- **实现期间发现并修掉一个跨变更缺陷**：summarize_node 用 eplan_capacity 决定
  "等着被重规划"，而路由用的是收窄后的条件，两者不一致时会在 plan_execute 模式下
  产出 **nswer="" + success=False**（已实测复现）。修法是把门控抽成单一真源
  pp/core/agent/graph/replan_gate.py，产出方与路由方共用。该缺陷属
  ix-agent-context-loss 的 gent/replan-context 需求（"被排除时 MUST 以已有信息
  给出诚实的部分答案"），修好后同一 e2e 变为 success=True 且答案非空。
