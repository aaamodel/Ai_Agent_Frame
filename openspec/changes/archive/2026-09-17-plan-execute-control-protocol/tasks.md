## 1. 回退子问题合并约束（第一问）

- [x] 1.1 将规划约束提示词改回「每个子问题必须派发自己的子任务」，移除「可由同一次工具调用覆盖时必须合并」的表述；验证：打印规划提示词确认约束文案已回退，`rag_knowledge_search` 类多子问题场景不再出现合并后的单一检索任务
- [x] 1.2 在规划输出结构中增加可选的「本子任务覆盖哪些子问题」声明字段（序号列表）；验证：用现有 response_format 工具生成 schema，确认 strict json_schema 合法且服务端可接受
- [x] 1.3 在计划解析阶段增加覆盖校验：子问题未被覆盖时触发一次重新规划，仍不覆盖则记录含子问题标识的告警并继续；验证：新增单测覆盖「全覆盖通过 / 缺失触发重试 / 重试仍缺失 fail-open 且留痕」三种分支

## 2. 计划台账（可见性）

- [x] 2.1 新增台账渲染纯函数，输入为计划与已执行记录，输出为每行的「标识 / 要解决的问题 / 工具或无工具 / 执行状态 / 是否解决本子问题」，执行状态区分已完成、正在执行、待执行、已跳过、失败，「是否解决」区分是/部分/否；验证：单测覆盖全部五种执行状态与三种解决判定，且断言台账不依赖任何额外状态字段
- [x] 2.2 在子任务执行前的模型输入中注入台账，替换或增强现有仅含结论文本的历史上下文；验证：执行一次多子任务场景，在 trace 中确认模型输入里能看到尚未执行子任务的工具与状态

## 3. 控制协议（结构化输出）

- [x] 3.1 新增控制协议输出结构（结论 + 是否解决本子问题自评 + 继续/提前收尾 + 跳过子任务列表 + 理由），沿用 `SummaryVerdictSchema` 的范式；验证：生成 strict json_schema 并确认三档 response_format 降级链可用
- [x] 3.2 将子任务提炼调用改为结构化输出并解析；解析失败但原始文本非空时保留该文本作为结论、控制指令按「继续」处理；执行失败（错误/空数据/熔断/审批拒绝）的步骤由程序强制将「是否解决」标为否，不采纳模型自评；验证：单测覆盖「正常解析 / 解析失败保留结论 / 跳过列表含非法 id 被忽略 / 失败步骤强制标记为否」
- [x] 3.3 state 新增跳过子任务列表与提前收尾标志两个字段并补齐初始值；验证：检查初始 state 组装结果含这两个键，且不影响现有 checkpoint 恢复

## 4. 路由与跳转

- [x] 4.1 路由改为计算「下一个未被跳过的下标」推进游标；验证：单测覆盖「跳过中间多个任务后直接跳到后续任务」
- [x] 4.2 提前收尾归一为「跳过剩余全部」并写入提前收尾标志；验证：单测确认剩余子任务全部不执行且进入汇总阶段
- [x] 4.3 重规划产出新计划时重置跳过记录与提前收尾标志；验证：单测确认新计划不继承旧的跳过结论

## 5. 可观测性、评测口径与回滚

- [x] 5.1 为每次跳过与提前收尾写入留痕（被跳过的子任务标识 + 理由）；验证：在 trace 中能查到该记录
- [x] 5.2 确认工具成功率与关键参数命中率只统计**实际执行**的步骤；验证：跑一次含跳过场景的冒烟，指标分母与执行步骤数一致
- [x] 5.3 增加编排开关包裹跳过与提前收尾的生效逻辑（默认开启）；验证：关闭开关后链路退回「跑完整计划」的既有行为
- [x] 5.4 同步更新优化纪要文档中关于子任务分配与执行控制的口径；验证：文档描述与实现一致

## 6. 端到端验证

- [ ] 6.1 跑一次多子问题场景（如同时问上线周期与实施费），确认：每个子问题都有对应子任务、答案覆盖全部子问题、单轮 token 与调用次数记录在案；验证：与变更前基线对比，覆盖无缺失且 token 涨幅在预期内
- [x] 6.2 跑一次「中途答案已足够」场景，确认模型能提前收尾且不执行剩余子任务；验证：图内集成测试 `test_control_finish_skips_remaining`（3 个取数子任务，第 1 个后提前收尾 → 工具只调用 1 次、summarize 只 1 次）通过
- [x] 6.3 回归现有测试套件；验证：`pytest evals 测试 benchmark -q` → **146 passed**
