## 1. 改写阶段产出目标

- [x] 1.1 在**基类**改写 schema 中新增 `agent_goal` 字段（描述控制在单行、不含示例，避免常驻抬高 response_format）；验证：生成 strict json_schema，断言基类与**组合 schema（子类）都包含该字段**且未在子类重复定义（继承生效），required 列表变化符合预期
- [x] 1.2 在**两个**改写提示词文件中增加该字段的输出要求与零容忍校验（必须描述最终交付物、禁止复述原始问题、禁止写成步骤列表）——主链路 `agent-rewrite-intent-combined.st` 与降级链路 `agent-question-rewrite.st` 都要改；验证：逐字检查两个文件的输出格式段字段清单均与 schema 一致，且约束文案都在
- [x] 1.3 在改写结果 DTO 中新增同名字段，并在解析层完成归一化（strip → 超长截断 → 空值回退为改写后问题）；验证：单测覆盖「正常值 / 空值回退 / 超长截断」三种分支

## 2. 承载与读取

- [x] 2.1 在 Pipeline 的槽位汇总处写入 `agent_goal`（沿用既有顶层槽位键的组织方式）；验证：跑一次 Pipeline 并确认最终 slots 中该键存在且值等于改写阶段产出
- [x] 2.2 新增统一的目标读取函数（从状态取目标 + 兜底），供所有注入点共用；验证：单测覆盖「槽位有值 / 槽位缺失 / 值为空串」三种情况，且断言兜底值与归一化层一致

## 3. 全链路注入

- [x] 3.1 在 ReAct 系统附加段注入目标（该函数同时服务 Function Calling 与文本协议）；验证：单测断言两种协议组装出的系统提示均包含目标
- [x] 3.2 在计划模式的规划提示词注入目标；验证：打印规划提示词确认目标存在，且与 ReAct 侧内容一致
- [x] 3.3 台账渲染支持目标作为**表格上方首行**（而非表格列），并在子任务执行提示词中传入；验证：单测断言目标出现在表格首行、且表格行内不重复出现该文字
- [x] 3.4 验证三处注入内容一致：用一个固定目标值分别组装三种提示词，断言其包含的目标文本完全相同（防止"一处用目标、一处用改写后问题"的漂移）

## 4. 兜底、可观测与验证

- [x] 4.1 目标缺失或为空时链路必须继续：验证：单测构造缺失目标的运行路径，断言流程完成且注入的是回退值
- [x] 4.2 记录一次目标产生与使用的留痕（便于抽查目标质量与排查注入不一致）；验证：trace 中能看到本轮目标值
- [x] 4.3 回归全部现有测试；验证：`pytest evals 测试 benchmark -q` 全通过（当前基线 146 passed）
- [x] 4.4 端到端实跑一次多子问题请求，确认：目标为"交付物形态"而非问题复述、三种提示词均已注入、单轮 token 增量在预期内（改写 schema 增约 120 字节 + 每次注入一行）；验证：与变更前基线对比并记录
