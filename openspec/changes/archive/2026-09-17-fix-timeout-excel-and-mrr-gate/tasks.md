## 1. 单次尝试独立预算

- [x] 1.1 在档位配置中新增"重试次数"字段（现有档位配置只有候选池与 `timeout_ms`），并给出保守默认值（1）与取值校验；验证：构造配置后能分别读出每档的"单次预算"与"重试次数"，未配置时取默认值且不报错
- [x] 1.2 客户端构建时关闭底层隐式重试；验证：断言构建出的客户端 `max_retries == 0`（此前为 SDK 默认 2）
- [x] 1.3 调用方改为**按每次请求**传入单次预算作为 HTTP 层超时（不可固化在客户端上——同一模型可能跨多个档位，如 glm-4.7 同时在 FAST 与 STANDARD）；验证：同一模型分别以 FAST / STANDARD 调用两次，断言两次请求携带各自档位的超时值
- [x] 1.4 执行器把"候选级一次 `wait_for`"改为"按尝试次数逐次 `wait_for`"，每次尝试各施放独立预算；外层 `wait_for` 取略大于单次预算的宽限值作为安全网；验证：单测模拟"首次尝试超时、第二次成功"，断言共发生 2 次尝试，且第二次未被第一次消耗预算
- [x] 1.5 逐次记录尝试的耗时与失败类型，使日志能区分"某次尝试超时"与"重试后仍失败"；验证：单测断言观测载荷含档位、单次预算、尝试序号与该次耗时
- [x] 1.6 纠正 `app/api/depends/dependencies.py` 中过期的超时口径注释（现写"15s"，实际配置为 50000ms）；验证：注释与 `.env` 的档位超时一致
- [x] 1.7 单测覆盖"预算耗尽后整体失败"：断言抛出可读错误且保留最后一次失败原因；验证：断言错误信息包含尝试次数与最后一次失败类型

## 2. 知识图谱抽取链路参数同源

- [x] 2.1 抽出唯一的"档位参数读取"入口：从档位配置读出（模型名、单次预算、重试次数、思考开关），替换 `light_rag.py` 中散落的独立取值与硬编码；验证：单测断言该入口对给定配置返回四项参数，且模型名取自候选池首个元素而非整串（整串会导致 404 model_not_found）
- [x] 2.2 让知识图谱抽取的模型调用使用上一步的超时与重试，替代其自身全局默认；验证：断言实际传入的单次超时等于档位预算（变更前为 240s 量级）
- [x] 2.3 保持其并发上限与响应缓存机制不变，确认不产生路由健康计数；验证：断言抽取阶段并发上限与缓存开关未被改动，且该链路不调用统一路由
- [x] 2.4 思考开关行为一致：要求关闭思考时，支持关闭的 provider 收到关闭指令、不支持关闭的模型跳过且不报错；验证：单测覆盖"可关闭"与"不可关闭"两类 provider，断言后者不注入关闭指令且无异常

## 3. 表格工具披露可聚合能力

- [x] 3.1 抽出"列可否参与数值聚合"的单一判定函数，供概览与聚合两侧共用（与聚合侧的数值转换口径同源）；验证：单测覆盖纯数值列、纯文本列、可转换文本列（带单位）三类，并断言判定为可聚合的列真实调用聚合能成功
- [x] 3.2 概览为每张表分别追加「可用作分组列」与「可聚合数值列」；某类为空时显式说明"无"而不留空；验证：单测断言"有分组列 / 无分组列"与"有数值列 / 无数值列"四种组合的输出都有明确标注
- [x] 3.3 概览给出可直接照抄的聚合参数示例，每张表至多一组；验证：单测断言示例数量上限，且按该示例真实调用聚合返回成功
- [x] 3.4 确认既有读取语义未变：筛选、明细、聚合的返回口径保持不变；验证：既有相关用例回归通过，且调用方使用非建议但合法的列名时照常执行、不被拒绝

## 4. 检索指标样本门

- [x] 4.1 新增检索类最小样本量常量，并**只应用于 `mrr`**（`recall@k` / `hit@k` 保持既有行为，见 D6 的取舍记录）；验证：单测断言样本数低于下限时 `mrr` 写入 `sample_insufficient`，而 `recall@k` / `hit@k` 未被标记
- [x] 4.2 判定与展示口径一致：被标记"样本量不足"的指标不得出现在违规列表；验证：单测构造仅 1 条可判分样本的检索结果 + 阈值，断言 `flatten_metrics` 不含 `mrr`、`evaluate_gate` 无 `mrr` 违规，而 `recall@5` 仍照常参与判定
- [x] 4.3 样本达标时照常判定；验证：单测以 ≥下限的样本数断言 `mrr` 参与判定，且 `sample_insufficient` 为空
- [x] 4.4 小样本冒烟不再假红：验证：`python -m evals.report --run-all --limit 2 --order random` 后 `rag.mrr_min` 不再出现于违规项，且报告标注"样本量不足"并给出所需条数
- [x] 4.5 全量结论不变：验证：`python -m evals.report --run-all` 中 mrr 仍参与判定（判分样本 27 ≥ 下限），数值与变更前一致（基线 mrr=0.699）

## 5. 回归与端到端验证

- [ ] 5.1 回归全部现有测试；验证：`pytest evals 测试 benchmark -q` 全通过（当前基线 178 passed）——**未达成**：本变更落地后为 214 passed + 1 failed（`evals/test_eval_gate.py::test_eval_gate_pytest_gate`）。该用例仅在 `evals/_results/latest.json` 存在时运行（我的 `--run-all` 生成了它），断言"真实评测结果必须过全部质量门"；唯一失败项为 `cost.avg_tokens_per_turn_max=20516 > 6000`，属**既有问题、非本变更引入**（同一份结果在变更前会多一条 `rag.mrr_min` 违规，已由本变更消除）。需另开变更处理 token 成本，或经确认后删除本地 `latest.json` 恢复该用例的"缺结果即跳过"行为
- [x] 5.2 端到端实跑，确认两处行为在真实链路上生效：① 一次模型调用内每次尝试各自独立计时（日志可见尝试序号与各自耗时）；② Excel 类请求的工具调用次数由 3 次降到 2 次（概览后可直接聚合）
- [x] 5.3 记录配置语义变更对 `.env.example` 的影响：档位超时由"整个候选预算"变为"单次尝试预算"，并补充重试次数配置项；验证：`.env.example` 的注释与实际解析字段一致
