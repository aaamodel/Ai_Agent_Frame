## 1. 步骤提示判定（根因，最先做）

- [x] 1.1 修正步骤提示的判定依据为词级：匹配到的步骤标记落在某个词内部时不算命中；验证：单测断言「我们优先做哪些行业？哪些行业算次优先？」「优先考虑华东区」「首先分析一下」均不再命中，而「先取数再对账最后出结论」「步骤1」「第2步」仍命中
- [x] 1.2 命中后只输出步骤语义片段，不再返回整句问题；去除步骤标记后不再表达步骤语义时视为未命中；验证：单测断言命中项的输出不含与步骤无关的问题原文，且退化输入不产出步骤提示
- [x] 1.3 端到端确认修复效果：用本次 trace 的原始问题构造改写请求，验证：断言不再产出 `explicit_plan_hint`，且不再以"显式步骤硬命中"强制进入计划模式（实测：explicit_plan_hint=None，decision_source 由 `explicit_hint` 变为 `rule_threshold`，mode 由 plan_execute 变为 react）

## 2. 规划器输入块（删冗余）

- [x] 2.1 冷启动参考段：与用户问题（或改写后的问题）逐字相同时整段不注入；验证：单测断言该情况下规划器提示词不含该段正文及其标题
- [x] 2.2 意图锚点：增加置信度门槛配置，低于门槛不注入为拆解方向约束，置信度缺失或非法一律按不足处理；验证：单测覆盖"达标 / 不足 / 缺失"三种输入，断言只有达标时注入且附带置信度
- [x] 2.3 技能说明：规划器侧只保留技能清单，不注入"如何读取技能文件"的操作步骤说明；验证：单测断言规划器提示词含清单条目（技能名 + 描述 + 源文件位置）且不含读取步骤说明
- [x] 2.4 修正技能段标题与内容的相邻关系，消除"标题下方直接跟另一个块的标题"的空标题现象；验证：单测断言技能段标题的下一个非空行属于技能清单

## 3. 技能文档结构化事实（防止信息丢失）

- [x] 3.1 在 `AgentGraphState` 新增存放运行期结构化事实的键并给默认值；验证：单测断言状态初始化后该键存在且为空，且读取侧在键缺失时按空处理不报错
- [x] 3.2 在执行层读取技能文档后做确定性提取，至少覆盖数据资产名称与其位置；验证：单测以真实 SKILL.md 的「数据资产地图」结构为输入，断言提取出的资产名与位置与文档逐字一致
- [x] 3.3 提取失败或文档无该结构时回退既有行为；验证：单测断言无表格文档下不中断、不注入空事实集合
- [x] 3.4 已提取事实必须对后续环节可见：重规划输入与后续子任务输入均能取到；验证：单测分别断言两处包含已提取的位置

## 4. 重规划输入完整性（防止信息丢失）

- [x] 4.1 修正执行结果归属的读取键（现读 `id`，权威键为 `subtask_id`），归属不可得时输出显式标记而非空值；验证：单测断言每条结果都带子任务标识，且不可得时为显式标记
- [x] 4.2 已试过的数据源、路径与查询结构化传递，并标明是否取得有效数据；验证：单测断言上一轮为空的路径出现在重规划输入中且标明为空，取得有效数据的尝试不被标记为无效
- [x] 4.3 工具失效判定改为依据是否取得有效数据，而非仅看状态字段；验证：单测断言"状态记为成功但未取到业务数据"的工具被计入失效工具、真实取得数据的工具不被计入
- [x] 4.4 重规划输入发出前做一次矛盾校验：失败原因中的建议方向若已被同一份输入的记录证明为空，则移除该建议并留痕；验证：单测断言矛盾建议被移除且留下记录，不矛盾的建议原样传递

## 5. 重规划触发收窄与次数上限

- [x] 5.1 `summarize_node` 输出**结构化**的缺口性质，至少区分"方向性错误（全部结论跑题）"与"未取得有效数据"；验证：单测构造两种情形，断言写入状态的缺口性质取值正确
- [x] 5.2 触发条件收窄：仅在缺口性质为方向性错误且本轮未重规划过时触发；缺口性质缺失或取值非法一律按"非方向性错误"处理；验证：单测覆盖"全跑题 → 允许""仅某步为空 → 禁止""性质缺失 → 禁止"三种输入
- [x] 5.3 触发前排除：所有候选工具在本轮已被证明未取得有效数据时禁止重规划；验证：单测断言该情形下路由到收尾而非重规划
- [x] 5.4 重规划次数硬上限改为 1；验证：单测断言已重规划过一次后再次判定不足时不触发重规划，并以已有部分答案诚实收尾
- [x] 5.5 被排除的判定必须留痕；验证：单测断言记录中含排除原因，可用于事后统计有多少请求因收窄而未走重规划（非方向性错误走 `replan.excluded` trace 事件；候选工具全部失效走 warning 日志——路由是纯函数拿不到 tracer，仅落日志）

## 6. 回归与端到端

- [x] 6.1 回归全部现有测试；验证：`pytest evals 测试 benchmark -q` 通过，且与变更前基线一致（当前存在 1 个既有的 cost gate 失败项，非本变更引入，不计入）（实测 307 passed + 1 既有失败）
- [x] 6.2 用本次 trace 的原始问题端到端实跑一次；验证：规划器提示词确认不含冷启动参考段、技能段无空标题；确认本轮不再白跑知识图谱检索；记录大模型调用次数、各次输入字符数与重规划次数，与变更前（13 次 / 约 37,400 字符 / 2 次重规划）对比
      **⚠️ 部分完成（受阻）**：真实链路（改写→意图→模式决策）与规划器提示词渲染已验证通过——
      `explicit_plan_hint=None`、`mode=react`、`decision_source=rule_threshold`、slots 无 hint 键；
      刻意注入"与问题逐字相同的 initial_plan_hint"后，规划器提示词确认不含冷启动参考段、
      不含整句问题复述、不含执行侧技能操作指引、技能段标题紧邻清单内容。
      **未完成**：完整状态图（含工具执行）无法在本机运行——**Milvus(19530) 未启动**，
      `knowledge_graph_search` 等工具依赖它，因此"不再白跑知识图谱检索"与
      "调用次数/输入字符数"两项无法实测。工具执行段的替代验证见
      `测试/test_graph_state_machine.py` 的图级测试（假模型，不含真实 Milvus）。
- [x] 6.3 构造一个方向性错误场景（全部结论跑题）；验证：重规划恰好发生 1 次，且其后再次判定不足时不再触发，改为诚实收尾（`test_off_topic_replans_exactly_once_then_stops`：初始 plan + 恰好 1 次 replan，汇总两次判不足但只重规划一次）

**注**：以上不含"执行期就地纠偏"相关任务——该部分已拆出到 `add-step-correction`。


---

## 7. 实现后补记（由 add-step-correction 的真实 e2e 反向查出并修复）

- **7.1 修复了一处会产生空答案的缺陷（已完成）**：summarize_node 用
  
eplan_capacity(state) 决定"写下不足信号、等着被重规划"，而路由
  
oute_after_summarize 用的是 §5 收窄后的条件。两者不一致时——
  summarize 把 inal_answer 置空，路由却按收窄条件**拒绝**重规划——
  用户拿到 nswer="" 且 success=False，直接违反 gent/replan-context 的
  "被排除时 MUST 以已有信息给出诚实的部分答案"。

  **实测复现**：强制 plan_execute 跑「我们优先做哪些行业」，2 个子任务、
  工具子任务全失败 → success=False、nswer=""。

  **修法**：把重规划门控抽成**单一真源** pp/core/agent/graph/replan_gate.py
  （REPLAN_HARD_LIMIT / INSUFFICIENCY_KIND_* / 
ormalize_gap_kind /
  ll_candidates_exhausted / 
eplan_allowed / 
eplan_exclusion_reason），
  由**产出方与路由方共用同一个函数**；被排除时 summarize 直接落到"诚实部分答案"
  收尾路径，不再留空答案。同时修正了 
eplan_allowed 在路由侧必须**从 state
  读取**不足信号（曾因默认 signal_present=True 使路由忽略信号而误判）。

  **修后同一 e2e**：success=True、degraded=False、答案非空
  （4 次调用 / 9,942 输入字符，对比变更前 13 次 / 约 37,400）。

- 6.2 在本次补记时**已跑通**（Milvus 可用）：真实端到端 strategy=auto →
  mode=react、success=True、工具序列 [file_read_tool, rag_knowledge_search]、
  **3 次调用 / 19,050 输入字符 / 0 次重规划**（对比变更前 13 次 / 约 37,400 / 2 次），
  且**未再出现知识图谱检索白跑**。原"受阻"记录保留在上方以便追溯。
