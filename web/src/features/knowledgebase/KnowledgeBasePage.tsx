/**
 * 知识库区（图谱集合管理）。
 *
 * ⚠️ 当前是**占位实现**：路由需要它存在才能编译（Task 9 装配路由时
 *    页面尚未创建）。Task 11 会用真正的页面替换本文件。
 */
export function KnowledgeBasePage() {
  return (
    <div className="p-6 text-sm text-neutral-500">知识库区（尚未实现）</div>
  );
}
