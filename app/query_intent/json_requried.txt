# 项目「显式要求大模型回复严格 JSON 输出」全量清单
# 生成时间：2026-09-02
# 总条目：11 处
#
# ============================================================
# 分类说明
# ============================================================
# A 类：Agent Pipeline 前置决策层（本轮新增 · 改写/意图/模式）
# B 类：原 RAG 改写 + 澄清 + MCP 参数提取层
# C 类：Agent 三大执行引擎层（Planner / Reflection / ReAct）
# D 类：工具路由层（ToolRouter）
#
# ============================================================
# 排除项（以下 JSON.loads 场景不列入 —— 它们不是对大模型输出的约束）
# ============================================================
# - app/infrastructure/cache/redis_cache.py 3 处（Redis 读缓存）
# - app/core/backends/filesystem.py L408（本地文件行 JSON 读取）
# - app/core/memory/short_term.py L110 / long_term.py L331（记忆对象反序列化）
# - app/core/tools/builtin/feishu.py L70（飞书配置 JSON 解析）
# - intent/intent_tree.py L37（意图树缓存加载）
# - rewrite/query_rewrite.py L96（改写服务缓存加载）
# - intent_prompt/agent_intent_prompt.py L169（Prompt 缓存加载）
# - intent/intent_classify.py L343（few-shot examples 预加载，非 LLM 输出）
# - app/core/agent/planner.py L159（tool_args_hint 复用解析，前序 Planner 产出不是新的 LLM 请求）

# ============================================================
# A 类：Agent Pipeline 前置决策层（本轮新增 · 3 条）
# ============================================================

-------------------------------------------------------------------
条目编号：A-1
功能模块：Agent 改写（Pipeline Stage1 · 改写+复杂度信号抽取）
触发调用：AgentMultiQuestionRewriteService.rewrite_for_agent(ctx)
          文件 rewrite/multi_question_rewrite_service.py 中的 public 方法
-------------------------------------------------------------------
【Prompt 模板约束】
  文件：prompts/agent-question-rewrite.st
  关键约束原文（4 处）：
    L10：输出：**一份 JSON 对象**，包含改写后问题、是否拆分子问题、复杂度分析、建议工具、显式步骤提示。
    L50-L51：## 输出格式（唯一允许格式）只输出一个 JSON 对象，不要输出任何多余文字，不要用 markdown 以外的解释。
    L53：```json 示例块
    L75：## 3 个零容忍校验（请自检 JSON 后再输出）
【要求的 JSON Schema 字段】
  顶层 dict，共 6 个 key：
    - rewrite: string                                 改写后的完整问题
    - should_split: bool                              是否需拆分子问题
    - sub_questions: list[string]                     拆分后的子问题数组
    - complexity_analysis: object                     复杂度分析（6 子字段）
        · estimated_steps: int，范围 1~10
        · estimated_tool_calls: int，范围 0~10
        · has_multi_step_dependency: bool
        · has_external_data_dependency: bool
        · need_creative_output: bool
        · reasoning_notes: string，中文 1~2 句
    - suggested_tools: list[string]                   必须从工具列表字面量挑
    - explicit_plan_hint: string | null               显式步骤提示（≤120 字）
【解析 / 消费方】
  函数：AgentMultiQuestionRewriteService._parse_agent_rewrite()
  位置：rewrite/multi_question_rewrite_service.py:L428-L493（及其后续行）
  解析逻辑：
    1. LLMResponseCleaner.strip_markdown_code_fence 去 ```json / ``` 围栏
    2. json.loads(cleaned_text)；非 dict → 返回 None
    3. 各字段严格上下界：estimated_steps ∈[1,10]，tool_calls ∈[0,10]
    4. suggested_tools 与 REGISTERED_ENABLED_TOOL_NAMES ∪ 请求快照做白名单交集过滤
  失败兜底：rewrite_for_agent fallback → RewriteResult(原始问题, should_split=False)

-------------------------------------------------------------------
条目编号：A-2
功能模块：意图分类（Pipeline Stage2 · 三通道打分节点召回）
触发调用：DefaultIntentClassifier.classify_targets(request, ...)
          文件 intent/intent_classify.py:L114-L149（B-1 dict 请求）
-------------------------------------------------------------------
【Prompt 模板约束】
  文件：prompts/intent-classifier.st
  关键约束原文：
    L69-L72：## 输出规范 / 格式要求：**只输出 JSON 数组，无其他文字**
    L73-L76：数组元素字段清单（id/score/reason）
    L78-L96：四类输出示例（正常 / 歧义 / 交互 / 空）
【要求的 JSON Schema 字段】
  顶层 list（JSON 数组），每个元素 dict：
    - id: string            意图节点 ID（必须在意图树中真实存在，否则跳过）
    - score: float          0~1 置信度（<0.6 且非歧义场景建议空数组）
    - reason: string        中文说明匹配理由
  支持"结果包"兼容：若顶层为 {"results": [...]} 则取 results 数组。
【解析 / 消费方】
  函数：DefaultIntentClassifier._parse_scores(raw, data, question)
  位置：intent/intent_classify.py:L151-L189
  解析逻辑：
    1. _strip_markdown_code_fence → json.loads
    2. isinstance(list) 或 dict["results"] → 否则 warning 空意图
    3. 逐项校验：必须含 id、score；id 必须在 data.id_to_node 内 → 否则跳过
    4. score 用 float() 转换，失败则跳过
    5. 最终按 score 降序排序返回 List[NodeScore]
  失败兜底：return []（空意图 → Stage3 聚合主意图="general"）

-------------------------------------------------------------------
条目编号：A-3
功能模块：编排模式决策（Pipeline Stage3 · 灰区 LLM 精判）
触发调用：ModeDecider._decide_by_llm_with_fallback(...)
          文件 orchestration/mode_decider.py:L237-L312
          （仅当规则阈值命中灰区才调用，约 10~20% 流量进入此 LLM）
-------------------------------------------------------------------
【Prompt 模板约束】
  文件：prompts/orchestration-mode-decider.st
  关键约束原文（3 处）：
    L13：本 Prompt 仅当"规则层在阈值边界拿不准"时才会被调用（因此需要精判，非兜底）
    L67-L68：## 输出格式（唯一允许格式）只输出一个 JSON 对象，不要输出任何额外解释文字，不要 markdown。
    L70：```json 示例块
    L80-L84：自检清单（mode 只能 "react" / "plan_execute" 二选一）
【要求的 JSON Schema 字段】
  顶层 dict，共 5 个 key：
    - mode: string                     Literal["react", "plan_execute"]（禁止任何变体）
    - confidence: float                0~1（2 位小数建议），低于 0.55 会被代码层丢弃
    - reason: string                   15~40 字中文决策理由（必填）
    - initial_plan_hint: string|null   仅 mode=plan_execute 填，3~5 条执行建议
    - first_tool_hint: string|null     仅 mode=react 填，必须为真实注册工具名
【解析 / 消费方】
  函数：ModeDecider._parse_llm_decision_json(raw_response_text, rewrite_result)
  位置：orchestration/mode_decider.py:L407-L467
  解析逻辑：
    1. strip_markdown_code_fence → json.loads；非 dict → None
    2. mode：任何非 "plan_execute"（大小写不敏感比较）一律保守归为 "react"
    3. confidence：max(0.0, min(1.0, float(...)))，再和 llm_min_confidence=0.55 比较
    4. initial_plan_hint：仅 plan_execute 模式读取，缺失时用 rewrite.explicit_plan_hint 兜底
    5. first_tool_hint：仅 react 模式读取，必须 ∈ REGISTERED_ENABLED_TOOL_NAMES 才保留
  失败兜底：return fallback（规则层默认或 MODE_DECISION_DEFAULT_MODE="react"，rag_constant.py:L42）

# ============================================================
# B 类：原 RAG 改写 + 澄清 + MCP 参数提取层（3 条）
# ============================================================

-------------------------------------------------------------------
条目编号：B-1
功能模块：RAG 查询改写（原有 RAG 检索用 · 与 Agent 改写模板独立并行）
触发调用：MultiQuestionRewriteService.rewrite_with_split(question, history)
          文件 rewrite/multi_question_rewrite_service.py:_parse_rewrite_and_split
-------------------------------------------------------------------
【Prompt 模板约束】
  文件：prompts/user-question-rewrite.st
  关键约束原文（2 处）：
    L12-L14：# 输出格式 / 严格返回 JSON，不要额外文字：
    L15-L19：```json 块示例
【要求的 JSON Schema 字段】
  顶层 dict，共 3 个 key：
    - rewrite: string
    - should_split: bool
    - sub_questions: list[string]
【解析 / 消费方】
  函数：MultiQuestionRewriteService._parse_rewrite_and_split(raw)
  位置：rewrite/multi_question_rewrite_service.py:L120-L146
  解析逻辑：
    1. LLMResponseCleaner.strip_markdown_code_fence → json.loads
    2. 非 dict → None；rewrite 空 → None
    3. sub_questions 缺失时 fallback 为 [rewrite] 单元素数组
  失败兜底：return None → 外层使用 rewrite 原问题 + _rule_based_split() 正则兜底

-------------------------------------------------------------------
条目编号：B-2
功能模块：意图歧义澄清检查（RAG 层 · 判断是否需要反问用户在多条候选路径中选一条）
触发调用：AmbiguityLLMChecker.check_ambiguity(question, ranked)
          文件 guidance/guidance_decision_checker.py:L33-L70
-------------------------------------------------------------------
【Prompt 模板约束】
  文件：prompts/guidance-ambiguity-check.st
  关键约束原文（1 处）：
    L20-L21：以 JSON 格式输出：{"ambiguous": true/false, "category_ids": [...], "reason": "..."}
【要求的 JSON Schema 字段】
  顶层 dict，共 3 个 key：
    - ambiguous: bool               用户需不需要被反问澄清
    - category_ids: list[string]    最匹配候选意图 ID（用于引导用户选择）
    - reason: string                中文判断理由
【解析 / 消费方】
  函数：AmbiguityLLMChecker.check_ambiguity → inline json.loads
  位置：guidance/guidance_decision_checker.py:L50-L70
  解析逻辑：
    1. strip_markdown_code_fence → json.loads
    2. 非 dict → warning + 返回 False（不澄清，不阻塞 RAG）
    3. 缺 "ambiguous" 字段 → warning + 返回 False
  失败兜底：统一 return False（让 RAG 正常走检索，避免"拦路澄清"）

-------------------------------------------------------------------
条目编号：B-3
功能模块：MCP 工具参数提取（从用户问题中提取出某工具定义所需的参数值并 JSON 化）
触发调用：（工具调用前）MCPParameterExtractService 类（推断自 Prompt 模板角色声明 + 约束）
【Prompt 模板约束】
  文件：prompts/mcp-parameter-extract.st
  关键约束原文（3 处）：
    L1：你是工具参数提取器，任务是从用户问题中提取工具定义所需的参数，并以 JSON 格式输出。
    L54：**格式**：严格合法的 JSON 对象，键名和字符串值用双引号，无尾逗号，必要时转义
    L56：**禁止**：在 JSON 之外添加任何解释、注释或文本
  L58：示例：{"param_1": "value", "param_2": 123, "param_3": true}
【要求的 JSON Schema 字段】
  顶层 dict（**动态 schema**：随每次传入的工具定义 parameters 标签不同而变化）
  填充规则（Prompt 中明确定义的"参数提取逻辑"表）：
    · 必填+有默认 → 未提及时取 default
    · 必填+无默认 → 未提及时取 null
    · 非必填+有默认 → 未提及时取 default
    · 非必填+无默认 → 不输出（从 JSON 中省略）
  类型处理规则：Enum 映射 / 中文阿拉伯数字转换 / 布尔"要= true/不=false" / 日期映射
【解析 / 消费方】
  （MCP 参数提取服务直接 json.loads(strip_markdown_code_fence(resp))）
  失败兜底：回退为"空参数对象 {} + 使用 default 默认值全量填充"

# ============================================================
# C 类：Agent 三大执行引擎层（Planner / Reflection / ReAct，4 条）
# ============================================================

-------------------------------------------------------------------
条目编号：C-1
功能模块：PlannerAgent 初始计划生成（Plan-and-Execute 的 plan 阶段）
触发调用：PlannerAgent.plan(query, memory_context, skills_block)
          文件 app/core/agent/planner.py:L93-L127
-------------------------------------------------------------------
【Prompt 模板约束】
  变量：PLAN_SYSTEM_PROMPT
  位置：app/core/agent/planner.py:L22-L31（代码内字符串）
  关键约束原文（2 处）：
    L25：输出格式必须是**严格的 JSON 对象**，包含 subtasks 列表，例如：
    L26-L30：```json 示例
  用户消息尾部提示（L118）："请输出合理的 JSON 计划。"
【要求的 JSON Schema 字段】
  顶层 dict，1 个 key：
    - subtasks: list[object]       每个 SubTask 对象：
        · id: string                  任务编号（task_1 等）
        · title: string               任务标题
        · description: string         详细描述
        · action_type: "tool" | "reasoning"
        · tool_name: string|null      action_type=tool 时必填
        · tool_args_hint: string|null 工具参数 JSON hint 字符串
【解析 / 消费方】
  辅助：_extract_json_object(text)
    位置：app/core/agent/planner.py:L53-L61
    逻辑：先直接 json.loads(text)，失败则 regex 抓 \{\} 大段再 json.loads，仍失败抛 ValueError
  辅助：_parse_subtasks(data)
    位置：app/core/agent/planner.py:L64-L77
    逻辑：遍历 subtasks 数组 → 逐字段 cast 为正确类型 → 构造 SubTask dataclass 列表
  失败兜底：Planner 抛出异常后 plan() 捕获 → return [fallback SubTask]（直接 reasoning 类型回答）

-------------------------------------------------------------------
条目编号：C-2
功能模块：PlannerAgent 重计划（执行失败 / 异常后自动 replan 生成新计划）
触发调用：PlannerAgent.replan(plan, results, error)
          文件 app/core/agent/planner.py:L239-L273
-------------------------------------------------------------------
【Prompt 模板约束】
  变量：REPLAN_SYSTEM_PROMPT
  位置：app/core/agent/planner.py:L33-L34
  关键约束原文（1 处）：
    L34：输出格式同样为**严格的 JSON 对象**。
  用户消息尾部（L257）："请输出修订后的 JSON 计划。"
【要求的 JSON Schema 字段】
  与 C-1 完全相同：顶层 {subtasks: list[SubTask]}
【解析 / 消费方】
  复用 C-1：_extract_json_object(raw) → _parse_subtasks(data)
  位置：app/core/agent/planner.py:L260-L263
  失败兜底：replan_fallback → 一条 SubTask: "降级为单步推理"+ action_type=reasoning

-------------------------------------------------------------------
条目编号：C-3
功能模块：ReflectionAgent 反思审查（两种编排模式执行完后后处理）
触发调用：ReflectionAgent.reflect(user_query, agent_answer, evidence, trace_summary)
          文件 app/core/agent/reflection.py:L78-L128
-------------------------------------------------------------------
【Prompt 模板约束】
  变量：REFLECTION_SYSTEM_PROMPT
  位置：app/core/agent/reflection.py:L17-L34
  关键约束原文（1 处）：
    L19：## 你必须只输出一个 JSON 对象，格式如下：
    L20-L28：字段说明 + ```json 示例块
  用户消息尾部（L105）："请输出 JSON 审查结果。"
【要求的 JSON Schema 字段】
  顶层 dict，共 7 个 key：
    - quality_score: int                  0~100（<60 默认不通过，可配置 min_quality_to_pass）
    - is_complete: bool
    - likely_hallucination: bool
    - hallucination_reasons: list[string]  幻觉证据点
    - completeness_notes: string           是否遗漏关键要点
    - suggestions: list[string]            改进建议
    - summary: string                      一句中文审查结论
【解析 / 消费方】
  辅助：_extract_json(text)
    位置：app/core/agent/reflection.py:L59-L68
    逻辑：与 Planner._extract_json_object 相同（直接 loads → 失败 re 搜 \{\}）
  主调用：reflect() 内 json 解析后（L129 起）逐字段 int()/bool()/list() 强转 → ReflectionReport dataclass
  失败兜底：quality_score=50, is_complete=False, summary="反思流程异常，已降级为保守评分"

-------------------------------------------------------------------
条目编号：C-4
功能模块：ReActAgent 工具调用 Action Input 字段（Thought→Action→Action Input→Observation 循环）
触发调用：ReActAgent.run() 每一步的 LLM 输出 → _parse_react_step(text)
          文件 app/core/agent/react_agent.py:L71-L94
-------------------------------------------------------------------
【Prompt 模板约束】
  变量：REACT_SYSTEM_PROMPT
  位置：app/core/agent/react_agent.py:L19-L36
  关键约束原文（1 处）：
    L35：## 规则 / Action Input 必须是合法 JSON。
  输出格式块（L23-L31）：
    · 若需调用工具：Thought:<中文> + Action:<工具名> + Action Input:<JSON对象>
    · 若已有信息：Thought:<依据> + Final Answer:<答案>
【要求的 JSON Schema 字段】
  Action Input 一行内嵌入的**动态 JSON 对象**（随工具定义参数不同而变化）：
    · 顶层必须是合法 dict（JSON 对象）
    · key 名称必须与所选工具的 func 函数签名（或 BaseTool.args_schema）一一对应
    · 值类型必须符合参数声明（字符串 / 整数 / 布尔 / 数组 / 嵌套 dict）
【解析 / 消费方】
  函数：_parse_react_step(text)
  位置：app/core/agent/react_agent.py:L71-L94
  解析逻辑：
    L84-L93：
      1. regex r"Action:\s*(\S+)" 抓工具名
      2. regex r"Action Input:\s*(\{[\s\S]*\})" 抓 JSON 块（贪婪匹配最后一个 \}）
      3. json.loads(input_m.group(1)) → 成功：out["action_input"] = dict
         失败：out["action_input"] = {}，附加 out["parse_error"] = "Action Input 不是合法 JSON"
  失败兜底：本轮 ReAct 循环 action_input 为空对象 → Tool.invoke(name, {}) 尝试用默认参数调用；若报错则 observation 返回错误信息给下一轮 Thought 学习修正。

# ============================================================
# D 类：工具路由层（1 条）
# ============================================================

-------------------------------------------------------------------
条目编号：D-1
功能模块：ToolRouter LLM 语义路由（从大工具池中筛出最多 N 个最相关工具）
触发调用：ToolRouter._route_via_llm(query, available_tools, llm_client)
          文件 app/core/tools/router.py:L55-L101
          （仅 force_full_pass=false 且 len(tools)>max_tools 时触发；当前默认 force_full_pass=True，实际未启用）
-------------------------------------------------------------------
【Prompt 模板约束】
  构造位置：app/core/tools/router.py:L63-L67（tool_router_system_prompt 字符串）
  关键约束原文（2 处）：
    L65-L67："挑选出完成该任务绝对必要的工具名称列表（最多不超过 {N} 个）。\n"
              + "请严格以纯 JSON 数组格式返回，不要包含任何 Markdown 标记或额外解释，格式如：[\"tool_a\", \"tool_b\"]"
【要求的 JSON Schema 字段】
  顶层 list[string]：每个元素为候选池中 BaseTool.name 的精确字面量。
  最多元素数：self._max_tools（默认 5）
【解析 / 消费方】
  位置：app/core/tools/router.py:L80-L87
  解析逻辑：
    1. cleaned_res = response.replace("```json", "").replace("```", "") 手工去围栏
    2. json.loads(cleaned_res)
    3. 非 list → ValueError；再按 list 中的 tool name 反查 available_tools 中真实的 BaseTool 实例
    4. 一个没挑中 → 兜底 available_tools[:max_tools]（保证 Agent 不裸奔）
  失败兜底：except 分支 → available_tools[:max_tools]（全量前几位），并 logger.error

# ============================================================
# 条目汇总（便于一眼核对）
# ============================================================
# 编号 | 位置                          | LLM 约束形式         | JSON 顶层类型
# ---- | ----------------------------- | -------------------- | ----------------
# A-1  | prompts/agent-question-rewrite.st + _parse_agent_rewrite()        | Prompt .st 模板      | dict（6 key：rewrite/should_split/sub_qs/complexity/suggested/explicit）
# A-2  | prompts/intent-classifier.st + _parse_scores()                    | Prompt .st 模板      | list[dict(id,score,reason)]
# A-3  | prompts/orchestration-mode-decider.st + _parse_llm_decision_json()| Prompt .st 模板      | dict（5 key：mode/confidence/reason/plan_hint/first_tool）
# B-1  | prompts/user-question-rewrite.st + _parse_rewrite_and_split()     | Prompt .st 模板      | dict（3 key：rewrite/should_split/sub_questions）
# B-2  | prompts/guidance-ambiguity-check.st + check_ambiguity()           | Prompt .st 模板      | dict（3 key：ambiguous/category_ids/reason）
# B-3  | prompts/mcp-parameter-extract.st + MCP 参数服务 json.loads        | Prompt .st 模板      | dict（动态 schema，取决于工具 parameters）
# C-1  | planner.py PLAN_SYSTEM_PROMPT + _extract_json_object + _parse_subtasks | 代码内字符串 prompt | dict（1 key：subtasks → list[SubTask]）
# C-2  | planner.py REPLAN_SYSTEM_PROMPT + 复用 C-1 解析                    | 代码内字符串 prompt | dict（同 C-1 schema）
# C-3  | reflection.py REFLECTION_SYSTEM_PROMPT + _extract_json             | 代码内字符串 prompt | dict（7 key：质量分/完成/幻觉/原因/笔记/建议/总结）
# C-4  | react_agent.py REACT_SYSTEM_PROMPT + _parse_react_step(Line89)     | 代码内字符串 prompt | dict（动态 schema：取决于工具函数签名）
# D-1  | router.py tool_router_system_prompt + L80 json.loads               | 代码内字符串 prompt | list[string]（工具名数组）
# ============================================================
# 合计 11 条大模型 JSON 输出约束。
