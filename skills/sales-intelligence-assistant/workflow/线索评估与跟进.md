### 工作流 C：线索评估与跟进
1. 用户给出公司名 → 先 `sales_sql_query` 查台账定位该线索（行号、当前状态、历史跟进）；
2. 用户提供新背景 → 按销售方法论手册的 MEDDIC 六要素逐项评估，给出商机评分与等级；
3. 需要外部信息（客户公司规模、近期动态、组织变化）→ `web_search`，标注可信度；
4. 输出：商机评估卡（六要素逐项判定 + 证据）→ 建议动作（推进/搁置/放弃）→ 若用户确认，按台账更新协议写回台账；
5. 新线索录入：按字段字典口径在台账末尾追加（逐格写入），线索编号按"格式：LD-YYYYMM-序号"。
