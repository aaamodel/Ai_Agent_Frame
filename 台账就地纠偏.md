1. 先更正一处字段名
你说的"意图识别给出的 allowed_tool_list"——代码里没有这个键。真实字段名是：

code
state["active_tool_names"]     # prepare_node._resolve_tool_names() 写入
plan_node / execute_node / replan_node 都用它做白名单。已在 design 里写死这个名称，避免实现时猜错。

2. 证据缺口检测：比对的是这两个数据集
code
数据集 A（问题侧核心词）          数据集 B（本步返回内容）
┌──────────────────────┐        ┌──────────────────────┐
│ 按优先级取：          │        │ 本步 observation     │
│ ① agent_goal         │   VS   │（工具原始返回，       │
│ ② per_sub_questions  │        │  上游已截到 8000）   │
│ ③ top_*_node.full_path│       │                      │
│ ④ 已提取事实          │        │ 只比本步，           │
│ ⑤ 回退 user_input    │        │ 不掺历史结论         │
│ → jieba 分词+停用词   │        │                      │
└──────────────────────┘        └──────────────────────┘
coverage = |{w ∈ A : w 出现在 B 中}| / |A|
三个必须写对的细节（否则这个算法会失效）：

细节	为什么
只比本步，不掺历史结论	历史结论的高覆盖率会把"本步返回跑题"掩盖掉——这是最容易写错的地方
字面出现，不是语义相似	"行业""优先级"在销售记录里出现 0 次是离散覆盖问题；向量化会把"行业"和"领域"拉近，反而引入噪声
两个不置位条件：A 为空、B 为空	B 为空是纯推理步骤（无工具返回），覆盖率无意义；宁可漏触发不得误伤
jieba 已写入 D6（原方案是字符 n-gram 免依赖，现改为分词，并明确否决了向量化与复用 LightRAG 抽取器——后者是一次 3,058 字符的 LLM 调用）。

3. 资产差集：什么减什么
code
差集①（工具侧）= state["active_tool_names"]  −  subtask_results 出现过的 tool_name

差集②（资产侧）= 已提取的结构化事实(名称/位置/工具)  −  本轮已尝试过的资产
                                                      ↑
                                          用"反查"判定，不是"抽取路径"
这里我没用 DeepSeek 的思路。 它写的是 tried_paths = {r["path"] for r in state["attempted_paths"]}——但 attempted_paths 这个字段不存在，而且"从入参里抽路径"要猜参数名（file_path / path / dir 各不相同），脆弱且要维护清单。

我的做法是用已提取资产去反查：

code
把每条 action_input 的值拼成文本
→ 若某个资产的「完整位置」或「文件名」出现在其中 → 该资产本轮已尝试过
好处：不需要知道参数名、不需要判断"什么算路径"；且同时匹配完整位置与文件名——后者能覆盖"模型自己拼了目录但文件名写对"的情形。

action_input 是真实入参 dict（execute_node 记账写入），这个输入现成可用。

"已尝试过"就不进候选，不论成败：已成功→数据已拿到；已失败→已证否。这就是防重复试探的结构性保证。

注意：本 trace 白名单 6 个工具不含任何 Excel 工具，而资产全是 Excel → 求交后资产侧候选会直接为空。这是预期的，且要求留下告警（它会暴露"白名单过窄"这个独立问题）。

4. 两个专项测试已写进任务（第 7 节）
测试	fixture	断言
测试/test_step_correction_candidates.py	真实 SKILL.md 数据资产地图 + 真实 action_input	①未尝试资产进入候选 ②位置命中的被剔除 ③仅文件名命中也算已尝试 ④白名单无对应工具→排除+告警 ⑤空集不注入不中断
测试/test_step_correction_evidence_gap.py	trace 真实问题核心词（行业/优先级）+ trace 真实返回（销售记录 / 知识库无匹配）	①销售记录 vs 行业优先级 → 置位 ②匹配→不置位 ③A 空→不置位 ④B 空→不置位 ⑤不掺历史结论
两个算法都要求是纯函数，这样测试接口天然可独立运行。

5. 另外三条
jieba：已加入（任务 1.1 装依赖 + 1.2 停用词封装 + D6 改写）
工具能力索引：已写成 Non-Goal——"候选列表本身已带工具名与一句话用途，足以支撑选方向；常驻索引只增加固定开销，且会让模型分不清'该换方向'与'只是要知道有哪些工具'"
白名单：用 active_tool_names，已写进 D2
状态
code
add-step-correction   4/4 制品完成   27 项任务   6 需求 / 20 场景
validate: 通过
改动落在：spec.md（证据缺口那条重写为"两个数据集"）、design.md（D2 补差集实施、D6 改 jieba、Non-Goals 加索引排除、移除已定的 Open Question）、tasks.md（加依赖节 + 第 7 节专项测试）。


1. 真正会改写计划的必要条件
以下条件必须同时满足：

a/当前是 plan 形态

即 state["plan"] 存在，走 _execute_plan_step。

ReAct 形态 _execute_react_step 不涉及这套纠偏。

b/当前子任务正常进入 LLM 提炼

即 skip_distill == False。

如果本步工具返回错误、空数据、预算熔断、审批拒绝、执行异常，会 skip_distill=True，不会走后续的候选注入和 selected_alternative_id 解析。

这类坏结果只记账，继续推进 cursor，最后交给 L2 计划级闸门决定是否 replan。

c/系统判定“需要注入候选方向”

injection_reason() 返回非 None。

触发原因三选一：

上一步结果无效：result_is_ineffective(last)，例如错误、空数据等；

上一步自评未解决：上一步 solved 为 "no" 或 "partial"；

本步证据缺口：detect_evidence_gap() 置位。

正常成功且上一步自评已解决，且本步无证据缺口时，不注入。

d/纠偏配额还没耗尽

remaining_quota(state) > 0。

配额公式：

有剩余预算：correction_quota = int(remaining * 0.5)；

未设总预算上限：保守取 QUOTA_WHEN_UNLIMITED = 2。

已应用的纠偏记录数从 state["step_corrections"] 里扣减。

e/候选列表非空且模型能看到

build_candidates(state) 产出的候选至少有一个。

render_candidates() 渲染后非空，长度不超过 CANDIDATE_MAX_CHARS = 300。

候选来源：

工具侧：active_tool_names 减去本轮已调用工具；

资产侧：extracted_facts 中已提取、尚未尝试、且其 tool 在当前白名单内的资产。

白名单里没有可消费某资产的工具时，该资产会进 excluded，不会成为候选。

f/控制协议开启，模型输出选择字段

agent_plan_control_enabled 默认 True。

system prompt 要求模型在 JSON 中输出 selected_alternative_id，形如 asset:xxx 或 tool:xxx。

模型必须从本次注入的候选里选，不能自己编工具名、参数或路径。

如果模型没输出、输出 null、输出非法标识，都不会执行。

g/translate_candidate() 翻译成功

parse_candidate_id() 能解析出合法 kind:name；

该标识必须在本次注入的 candidates 列表中；

工具候选必须仍在白名单；

资产候选必须能在 facts 中找到，且 tool 在白名单、location 非空。

h/cursor 之后还有可替换的待执行子任务

next_pending_cursor(plan, cursor + 1, skipped_ids) 能找到目标。

纠偏只改 当前 cursor 之后 的第一个未跳过子任务；

不会改当前正在执行的步骤，也不会改已跳过记录或提前收尾标记。

如果后面没有待执行任务，直接跳过纠偏。

满足后，_apply_step_correction() 会把目标子任务改写为新的 tool 子任务：

设置 action_type="tool"；

设置 tool_name、tool_args_hint；

在 description 中追加“执行期就地纠偏：改用 xxx”；

写入 step_corrections 留痕，并触发 trace：plan_execute.correction_applied。